const fs = require('fs'); const code = fs.readFileSync('src/AdminPanel.jsx', 'utf8'); const start = code.indexOf('function CatalogManager'); const end = code.indexOf('function DownloadManager'); const catalogCode = code.substring(start, end); const imports = 'import React, { useState } from \'react\';\nimport { Trash2, Edit2, Check, X } from \'lucide-react\';\n\n'; fs.writeFileSync('src/CatalogManager.jsx', imports + catalogCode + '\nexport default CatalogManager;\n'); console.log('Extracted');
